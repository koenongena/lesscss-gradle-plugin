package projects.webapp
apply plugin: "lesscss"

lesscss {
    sourceDirectory = "${project.basedir}/src/main/webapp/less"
    outputDirectory = "${project.build.directory}/${project.build.finalName}/css"
}